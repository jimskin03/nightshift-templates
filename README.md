# Nightshift Templates

Static template showcase migrated from InfinityFree hosting.

## Local preview

Serve this directory with any static web server, for example:

```bash
python3 -m http.server 8080
```

## GitHub Pages

The `CNAME` file configures the custom domain:

`template.cryptgregresearch.org`
